# 🔧 Наверстка: что нужно дописать в бэкенде

> Дата обновления: 22.04.2026  
> Фронтенд использует Angular + `api.service.ts`, бэкенд — Django REST Framework (SQLite).  
> `djangorestframework-simplejwt 5.3.1` **уже установлен**.

---

## Текущее состояние бэкенда

| Что есть | Статус |
|---|---|
| `GET /api/pets/` — список питомцев | ✅ Работает |
| `GET /api/shelters/` — список приютов | ✅ Работает |
| `POST/PATCH/DELETE /api/pets/` | ✅ Работает (но без авторизации!) |
| `GET /api/stats/` | ❌ Не реализовано |
| `POST /api/auth/login/` | ❌ Не реализовано |
| `POST /api/auth/register/` | ❌ Не реализовано |
| `POST /api/auth/refresh/` | ❌ Не реализовано |
| `POST/DELETE /api/pets/{id}/favorite/` | ❌ Не реализовано |
| `GET /api/favorites/` | ❌ Не реализовано |
| `POST /api/contact/` | ❌ Не реализовано |
| `GET /api/team/` | ❌ Не реализовано |
| `GET /api/services/` | ❌ Не реализовано |

### Несоответствия моделей

Фронтенд ожидает поля, которых **нет** в текущих моделях:

| Фронтенд ожидает | Модель `Pet` имеет | Проблема |
|---|---|---|
| `image` | `photo` | ❌ Разное название поля |
| `is_adopted` | — | ❌ Поля нет |
| `video` | — | ❌ Поля нет |
| `age` (строка типа "2 года") | `age` (число int) | ⚠️ Тип другой |
| `created_at` | — | ❌ Поля нет |

| Фронтенд ожидает | Модель `Shelter` имеет | Проблема |
|---|---|---|
| `location` | `address` | ❌ Разное название |
| `image` | `photo` | ❌ Разное название |
| `pet_count` | — | ❌ Нет |
| `rating` | — | ❌ Нет |
| `tag` | — | ❌ Нет |
| `email` | — | ❌ Нет |
| `created_at` | — | ❌ Нет |

---

## Шаг 1: Починить модель `Pet` в `animals/models.py`

Добавить недостающие поля и переименовать `photo` → `image`:

```python
from django.db import models
from shelters.models import Shelter


class SpeciesChoices(models.TextChoices):
    CAT = 'cat', 'Кошка'
    DOG = 'dog', 'Собака'
    OTHER = 'other', 'Другое'


class GenderChoices(models.TextChoices):
    MALE = 'male', 'Мальчик'
    FEMALE = 'female', 'Девочка'


class Pet(models.Model):
    shelter = models.ForeignKey(
        Shelter, on_delete=models.CASCADE,
        related_name='animals', verbose_name="Приют"
    )
    name = models.CharField(max_length=100, verbose_name="Кличка")
    species = models.CharField(max_length=20, choices=SpeciesChoices.choices)
    breed = models.CharField(max_length=100, blank=True, default='')
    gender = models.CharField(max_length=10, choices=GenderChoices.choices, default=GenderChoices.MALE)
    age = models.CharField(max_length=50, blank=True, default='')  # строка: "2 года"
    description = models.TextField(blank=True)
    image = models.URLField(blank=True, default='')   # URL картинки (не файл)
    video = models.URLField(blank=True, default='')   # URL видео
    is_adopted = models.BooleanField(default=False)
    created_at = models.DateTimeField(auto_now_add=True)

    def __str__(self):
        return f"{self.name} ({self.get_species_display()})"
```

После изменения:
```bash
python manage.py makemigrations
python manage.py migrate
```

---

## Шаг 2: Починить модель `Shelter` в `shelters/models.py`

```python
from django.db import models


class Shelter(models.Model):
    name = models.CharField(max_length=200)
    location = models.CharField(max_length=255, blank=True, default='')  # было address
    phone = models.CharField(max_length=20, blank=True, default='')
    email = models.EmailField(blank=True, default='')
    description = models.TextField(blank=True)
    image = models.URLField(blank=True, default='')   # было photo
    tag = models.CharField(max_length=100, blank=True, default='')
    rating = models.FloatField(default=0.0)
    created_at = models.DateTimeField(auto_now_add=True)

    @property
    def pet_count(self):
        return self.animals.count()

    def __str__(self):
        return self.name
```

После изменения:
```bash
python manage.py makemigrations
python manage.py migrate
```

---

## Шаг 3: Обновить сериализаторы

### `animals/serializers.py`
```python
from rest_framework import serializers
from .models import Pet


class PetSerializer(serializers.ModelSerializer):
    gender_display = serializers.CharField(source='get_gender_display', read_only=True)
    shelter_name = serializers.CharField(source='shelter.name', read_only=True)

    class Meta:
        model = Pet
        fields = [
            'id', 'name', 'breed', 'age', 'species', 'gender', 'gender_display',
            'shelter', 'shelter_name', 'image', 'video',
            'description', 'is_adopted', 'created_at'
        ]
```

### `shelters/serializers.py`
```python
from rest_framework import serializers
from .models import Shelter


class ShelterSerializer(serializers.ModelSerializer):
    pet_count = serializers.IntegerField(read_only=True)

    class Meta:
        model = Shelter
        fields = [
            'id', 'name', 'location', 'image', 'pet_count',
            'rating', 'tag', 'description', 'phone', 'email', 'created_at'
        ]
```

---

## Шаг 4: Добавить модель `Favorite` в `animals/models.py`

Дописать внизу файла (после класса `Pet`):

```python
from django.contrib.auth.models import User

class Favorite(models.Model):
    user = models.ForeignKey(User, on_delete=models.CASCADE, related_name='favorites')
    pet = models.ForeignKey(Pet, on_delete=models.CASCADE, related_name='favorited_by')
    created_at = models.DateTimeField(auto_now_add=True)

    class Meta:
        unique_together = ('user', 'pet')

    def __str__(self):
        return f"{self.user.username} → {self.pet.name}"
```

```bash
python manage.py makemigrations
python manage.py migrate
```

---

## Шаг 5: Добавить модель `ContactMessage` в `shelters/models.py`

Дописать внизу:

```python
class ContactMessage(models.Model):
    name = models.CharField(max_length=100)
    phone = models.CharField(max_length=20)
    message = models.TextField()
    created_at = models.DateTimeField(auto_now_add=True)

    def __str__(self):
        return f"Сообщение от {self.name}"
```

```bash
python manage.py makemigrations
python manage.py migrate
```

---

## Шаг 6: Написать Views в `animals/views.py`

Заменить весь файл:

```python
from rest_framework import viewsets, status
from rest_framework.decorators import action, api_view, permission_classes
from rest_framework.permissions import IsAuthenticated, AllowAny
from rest_framework.response import Response
from django.contrib.auth.models import User
from .models import Pet, Favorite
from .serializers import PetSerializer, FavoriteSerializer


class PetViewSet(viewsets.ModelViewSet):
    queryset = Pet.objects.all()
    serializer_class = PetSerializer

    def get_queryset(self):
        qs = Pet.objects.all()
        shelter = self.request.query_params.get('shelter')
        if shelter:
            qs = qs.filter(shelter__id=shelter)
        return qs

    @action(detail=True, methods=['post', 'delete'], permission_classes=[IsAuthenticated])
    def favorite(self, request, pk=None):
        pet = self.get_object()
        if request.method == 'POST':
            fav, created = Favorite.objects.get_or_create(user=request.user, pet=pet)
            msg = 'Добавлено в избранное' if created else 'Уже в избранном'
            return Response({'message': msg}, status=status.HTTP_200_OK)
        elif request.method == 'DELETE':
            Favorite.objects.filter(user=request.user, pet=pet).delete()
            return Response({'message': 'Удалено из избранного'}, status=status.HTTP_200_OK)


class FavoriteViewSet(viewsets.ReadOnlyModelViewSet):
    serializer_class = FavoriteSerializer
    permission_classes = [IsAuthenticated]

    def get_queryset(self):
        return Favorite.objects.filter(user=self.request.user)


@api_view(['GET'])
@permission_classes([AllowAny])
def get_stats(request):
    from shelters.models import Shelter
    return Response({
        'shelters_count': Shelter.objects.count(),
        'pets_count': Pet.objects.count(),
        'adopted_count': Pet.objects.filter(is_adopted=True).count(),
        'total_pets': Pet.objects.count(),
    })


@api_view(['POST'])
@permission_classes([AllowAny])
def register_view(request):
    username = request.data.get('username')
    email = request.data.get('email')
    password = request.data.get('password')
    if not username or not password:
        return Response({'error': 'Заполните все поля'}, status=400)
    if User.objects.filter(username=username).exists():
        return Response({'username': ['Пользователь уже существует']}, status=400)
    user = User.objects.create_user(username=username, email=email, password=password)
    return Response({'message': 'Зарегистрирован'}, status=201)
```

---

## Шаг 7: Добавить `FavoriteSerializer` в `animals/serializers.py`

Дописать внизу файла:

```python
from .models import Favorite

class FavoriteSerializer(serializers.ModelSerializer):
    pet_detail = PetSerializer(source='pet', read_only=True)

    class Meta:
        model = Favorite
        fields = ['id', 'pet', 'pet_detail', 'created_at']
```

---

## Шаг 8: Обновить `shelters/views.py`

```python
from rest_framework import viewsets, status
from rest_framework.decorators import api_view, permission_classes
from rest_framework.permissions import AllowAny
from rest_framework.response import Response
from .models import Shelter, ContactMessage
from .serializers import ShelterSerializer


class ShelterViewSet(viewsets.ModelViewSet):
    queryset = Shelter.objects.all()
    serializer_class = ShelterSerializer


@api_view(['POST'])
@permission_classes([AllowAny])
def contact_view(request):
    name = request.data.get('name', '')
    phone = request.data.get('phone', '')
    message = request.data.get('message', '')
    if not name or not message:
        return Response({'error': 'Заполните обязательные поля'}, status=400)
    ContactMessage.objects.create(name=name, phone=phone, message=message)
    return Response({'message': 'Сообщение получено!'}, status=201)
```

---

## Шаг 9: Обновить `core/urls.py`

```python
from django.contrib import admin
from django.urls import path, include
from rest_framework import routers
from rest_framework_simplejwt.views import TokenObtainPairView, TokenRefreshView
from animals.views import PetViewSet, FavoriteViewSet, get_stats, register_view
from shelters.views import ShelterViewSet, contact_view

router = routers.DefaultRouter()
router.register(r'pets', PetViewSet)
router.register(r'shelters', ShelterViewSet)
router.register(r'favorites', FavoriteViewSet, basename='favorite')

urlpatterns = [
    path('admin/', admin.site.urls),
    path('api/', include(router.urls)),
    path('api/stats/', get_stats),
    path('api/contact/', contact_view),
    path('api/auth/login/', TokenObtainPairView.as_view(), name='token_obtain_pair'),
    path('api/auth/refresh/', TokenRefreshView.as_view(), name='token_refresh'),
    path('api/auth/register/', register_view),
]
```

---

## Шаг 10: Обновить `core/settings.py`

Добавить блок `REST_FRAMEWORK` и настройки JWT:

```python
from datetime import timedelta

REST_FRAMEWORK = {
    'DEFAULT_AUTHENTICATION_CLASSES': (
        'rest_framework_simplejwt.authentication.JWTAuthentication',
    ),
    'DEFAULT_PERMISSION_CLASSES': (
        'rest_framework.permissions.AllowAny',
    ),
    'DEFAULT_PAGINATION_CLASS': 'rest_framework.pagination.PageNumberPagination',
    'PAGE_SIZE': 100,
}

SIMPLE_JWT = {
    'ACCESS_TOKEN_LIFETIME': timedelta(minutes=60),
    'REFRESH_TOKEN_LIFETIME': timedelta(days=7),
}
```

---

## Порядок выполнения

1. `animals/models.py` — правим поля Pet, добавляем Favorite
2. `shelters/models.py` — правим поля Shelter, добавляем ContactMessage
3. `animals/serializers.py` — обновляем PetSerializer, добавляем FavoriteSerializer
4. `shelters/serializers.py` — обновляем ShelterSerializer
5. `animals/views.py` — полная замена
6. `shelters/views.py` — полная замена
7. `core/urls.py` — полная замена
8. `core/settings.py` — добавить REST_FRAMEWORK + SIMPLE_JWT блоки
9. Запустить:
   ```bash
   python manage.py makemigrations
   python manage.py migrate
   python manage.py runserver
   ```

---

## Что НЕ нужно устанавливать

`djangorestframework-simplejwt` **уже установлен** (v5.3.1) ✅  
`djangorestframework`, `corsheaders` — уже установлены ✅

---

## Эндпоинты после реализации

| Метод | URL | Авторизация | Что делает |
|---|---|---|---|
| GET | `/api/pets/` | Нет | Список питомцев |
| GET | `/api/pets/?shelter=1` | Нет | Питомцы приюта |
| PATCH | `/api/pets/{id}/` | Нет | Изменить питомца |
| POST | `/api/pets/{id}/favorite/` | JWT | Добавить в избранное |
| DELETE | `/api/pets/{id}/favorite/` | JWT | Убрать из избранного |
| GET | `/api/shelters/` | Нет | Список приютов |
| GET | `/api/favorites/` | JWT | Моё избранное |
| GET | `/api/stats/` | Нет | Статистика платформы |
| POST | `/api/contact/` | Нет | Форма обратной связи |
| POST | `/api/auth/login/` | Нет | Получить JWT токен |
| POST | `/api/auth/refresh/` | Нет | Обновить токен |
| POST | `/api/auth/register/` | Нет | Регистрация |
